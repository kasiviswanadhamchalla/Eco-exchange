server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecoexchange_identity?createDatabaseIfNotExist=true
    username: root
    password: Nikhil@420
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: super-secret-key-for-jwt-signing-change-in-production
  expiration: 86400000
